<!-- SPDX-License-Identifier: MIT -->

# Database Module - Visual Architecture

## 🏗️ Module Architecture

```c
┌─────────────────────────────────────────────────────────────────┐
│                     Application Entry Point                      │
│                        (index.ts / main)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│          DatabaseManager (Singleton)          ✨ NEW            │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ • Lazy initialization                                    │    │
│  │ • Initialization hooks                                   │    │
│  │ • Safe access patterns                                   │    │
│  │ • Query shortcuts                                        │    │
│  └─────────────────────────────────────────────────────────┘    │
└────────────────┬──────────────────────────────┬─────────────────┘
                 │                              │
                 ▼                              ▼
    ┌────────────────────────┐    ┌──────────────────────────┐
    │  DatabaseRouterFactory │    │   Utility Functions      │
    │   ✨ NEW               │    │   ✨ NEW (10+)           │
    ├────────────────────────┤    ├──────────────────────────┤
    │ • GET/POST/PUT/DELETE  │    │ • parsePagination()      │
    │ • DB context injection │    │ • buildWhereClause()     │
    │ • Auto error handling  │    │ • formatDatabaseError()  │
    │ • Request logging      │    │ • retryOperation()       │
    │ • Middleware support   │    │ • batchOperations()      │
    │                        │    │ • transactionPattern()   │
    │ ┌────────────────────┐ │    │ • ... and more           │
    │ │ Express Router     │ │    └──────────────────────────┘
    │ └────────────────────┘ │
    └────────────────────────┘
                 │
                 ▼
    ┌────────────────────────┐
    │  Your Route Handlers   │
    │  ┌────────────────────┐│
    │  │ factory.get('/', │ │
    │  │   async (r,s,db) │ │
    │  │ )                  ││
    │  └────────────────────┘│
    └────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │      Core Database Services            │
    ├────────────────────────────────────────┤
    │ • db.ts (Service Interface)            │
    │ • dbService.ts (Implementation)        │
    │ • database.ts (Type Definitions)       │
    │ • postgres.ts (PostgreSQL Support)     │
    └────────────────────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │        SQLite / PostgreSQL             │
    │          Database Engine               │
    └────────────────────────────────────────┘
```

## 🔄 Request Flow with DatabaseRouterFactory

```c
┌──────────────────────────────────────────────────────────┐
│  HTTP Request (GET /api/users)                           │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  DatabaseRouterFactory Middleware                        │
│  • Check if DB initialized                              │
│  • Wait for initialization if needed ✨                 │
│  • Pass control to route handler                        │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Route Handler (with DatabaseContext)                   │
│                                                          │
│  async (req, res, db) => {                             │
│    // db automatically injected ✨                      │
│    const users = await db.query(sql);                 │
│  }                                                      │
└────────────────────┬─────────────────────────────────────┘
                     │
         ┌───────────┼───────────┐
         ▼           ▼           ▼
    ┌──────────┐ ┌──────────┐ ┌──────────┐
    │ db.query │ │queryOne()│ │execute() │
    └──────────┘ └──────────┘ └──────────┘
         │           │           │
         └───────────┼───────────┘
                     ▼
         ┌──────────────────────────┐
         │ DatabaseManager.query()  │
         │ (Safe access, waits      │
         │  for initialization)     │
         └───────────┬──────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │   dbService.all()        │
         │   dbService.get()        │
         │   dbService.run()        │
         └───────────┬──────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │   better-sqlite3         │
         │   or PostgreSQL Client   │
         └───────────┬──────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │   Database Result        │
         └───────────┬──────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │ Format Response          │
         │ (JSON)                   │
         └───────────┬──────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  HTTP Response (200 OK)                                  │
└──────────────────────────────────────────────────────────┘
```

## 📊 Initialization Timeline

```c
Application Start
        │
        ▼
┌─────────────────┐
│ index.ts starts │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────┐
│ registerDatabaseHooks()     │
│ (Register any setup hooks)  │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ initializeDatabase()        │
│ • Calls db.init()          │
│ • Runs all hooks           │
│ • Sets initialized = true  │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Express App Ready           │
│ • Middleware setup         │
│ • Routes registered        │
│ • Listen on port           │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ First Request               │
│ • DB already initialized    │
│ • Fast response             │
└─────────────────────────────┘
```

## 🔀 Initialization Patterns

### Pattern 1: DatabaseRouterFactory (Recommended)

```c
Router Factory Created
        │
        ▼
┌──────────────────────────────┐
│ Factory Middleware           │
│ await db.initialize()        │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Route Handler Called         │
│ db context injected ✨       │
└──────────┬───────────────────┘
           │
           ▼
    Response Sent
```

### Pattern 2: DatabaseManager Direct

```c
Manual Route
        │
        ▼
┌──────────────────────────────┐
│ await DatabaseManager.       │
│   initialize()               │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Use DatabaseManager methods  │
│ • query()                    │
│ • queryOne()                 │
│ • execute()                  │
└──────────┬───────────────────┘
           │
           ▼
    Response Sent
```

### Pattern 3: Safe Access

```c
Any Operation
        │
        ▼
┌──────────────────────────────┐
│ await DatabaseManager.       │
│   safeAccess(async () => {}) │
└──────────┬───────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
 Initialized   Not Initialized
    │             │
    │             ▼
    │      ┌──────────────┐
    │      │ Initializing │
    │      │ (wait)       │
    │      └──────┬───────┘
    │             │
    └─────────┬───┘
              │
              ▼
      ┌──────────────┐
      │ Run Callback │
      └──────┬───────┘
             │
             ▼
        Return Result
```

## 🧩 Module Dependencies

```c
index.ts (exports)
├── DatabaseManager.ts
│   ├── database.ts (types)
│   └── dbService.ts
├── DatabaseRouterFactory.ts
│   ├── DatabaseManager.ts
│   └── database.ts (types)
├── utils.ts
│   └── database.ts (types)
├── db.ts
│   └── dbService.ts
├── database.ts (types)
└── postgres.ts (re-exports database.ts)
```

## 📈 Data Flow: Query Execution

```c
User Code
    │
    ├─► await db.query('SELECT * FROM users')
    │
    ▼
DatabaseRouterFactory (or DatabaseManager)
    │
    ├─► Check if initialized
    │
    ├─► If not, call initialize()
    │   ├─► Create connection
    │   ├─► Run migrations
    │   └─► Set initialized = true
    │
    ▼
DatabaseManager.query()
    │
    ├─► Call safeAccess()
    │   ├─► Wait for initialization
    │   └─► Execute callback
    │
    ▼
dbService.all() (internal)
    │
    ├─► Prepare statement
    │
    ├─► Execute query
    │
    ├─► Format results
    │
    ▼
Return Results to User
```

## 🔒 Error Handling Flow

```c
Operation
    │
    ├─► Error Occurs
    │   ├─► Database Error
    │   ├─► Connection Error
    │   └─► Query Error
    │
    ▼
DatabaseRouterFactory (catches)
    │
    ├─► formatDatabaseError()
    │   ├─► Check error type
    │   ├─► Create user-friendly message
    │   └─► Log detailed error
    │
    ▼
HTTP Response
    │
    ├─► 400: Bad Request
    ├─► 500: Server Error
    └─► 503: Service Unavailable
```

## 🧪 Testing Architecture

```c
Test Suite
    │
    ├─► beforeAll()
    │   └─► await DatabaseManager.initialize()
    │
    ├─► Test Cases
    │   ├─► await DatabaseManager.query()
    │   ├─► await DatabaseManager.execute()
    │   └─► Check results
    │
    └─► afterAll()
        └─► Cleanup
```

---

**Visualization Date**: 2025-12-20  
**Status**: Complete and Accurate
